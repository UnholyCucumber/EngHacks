class Course:	
	def __init__(self, name, classNumber, start, end, classType):
		self.name = name
		self.start = start
		self.end = end
		self.classNumber = classNumber
		self.classType = classType
		